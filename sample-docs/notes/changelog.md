# Changelog

This file lives in a **sub-folder** (`notes/`), so it appears inside a folder
node in the sidebar tree — expand `notes` to reach it, or arrive here via the
internal link on the [home page](../index.md) (the tree reveals this file
automatically).

## Entries

- **v0.2** — folder tree sidebar with drilldown, plus a refresh button.
- **v0.1** — initial viewer: pick a folder, browse, render.

Back to the [home page](../index.md).
